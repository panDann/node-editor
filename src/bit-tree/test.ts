import { BitTree, BitNode } from './base-class';

// const node = new BitNode(0)
export default function () {
    // const b = new BitTree(node)
    let n = 1000
    // while(n){
    //     b.insertLeftAt( node,n--)
    //     b.insertRightAt(node,n--)
    // }
    
}